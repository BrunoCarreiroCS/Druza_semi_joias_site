.claude/

# Config com chaves reais do Supabase (NÃO versionar)
js/config.js

# Variáveis de ambiente do backend de pagamento (Fase 4d)
.env
.env.local

# Artefatos de ferramentas locais (rascunho e capturas de auditoria) —
# ficam no disco, nunca no repositório.
.superpowers/brainstorm/
output/playwright/
.impeccable/
